# Настройка Git для прошивки Naviga

## Текущая ситуация

- Версия: `v2.6.11.60ec05e` (detached HEAD)
- Изменения: `src/mesh/RadioInterface.cpp` (Малайзия: 22 dBm)
- Цель: Обновиться до `v2.7.14+` с сохранением изменений

## Решение: Создать свою ветку

### Вариант 1: Автоматически (рекомендуется)

```bash
cd <корень репо Naviga>
./tools/setup_naviga_branch.sh
```

### Вариант 2: Вручную

Выполните команды по порядку:

```bash
cd meshtastic-firmware

# 1. Создать свою ветку и сохранить изменения
git checkout -b naviga-custom
git add src/mesh/RadioInterface.cpp
git commit -m "Naviga: Malaysia region TX power 22 dBm for T-Beam"

# 2. Настроить upstream
git remote add upstream https://github.com/meshtastic/firmware.git
git fetch upstream

# 3. Проверить доступные версии
git tag | grep "v2.7" | tail -5
```

## Обновление до v2.7.14

После настройки ветки:

```bash
# 1. Создать временную ветку для обновления
git checkout -b naviga-update-2.7.14 upstream/v2.7.14

# 2. Применить свои изменения
git merge naviga-custom

# 3. Если есть конфликты - разрешить их
# (Git покажет какие файлы нужно отредактировать)

# 4. После разрешения конфликтов:
git add <файлы>
git commit

# 5. Обновить основную ветку
git checkout naviga-custom
git merge naviga-update-2.7.14

# 6. Удалить временную ветку
git branch -d naviga-update-2.7.14
```

## Структура

```
naviga-custom (ваша основная ветка)
├─ Изменения для Малайзии (22 dBm) ✅
├─ GPS улучшения (будет добавлено) 📝
└─ Другие изменения Naviga
```

## Документация

- **Быстрый старт**: `docs/QUICK_START_Git.md`
- **Полная документация**: `docs/Git_Workflow_For_Fork.md`
