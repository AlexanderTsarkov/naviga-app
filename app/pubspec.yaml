name: naviga_app
description: "Naviga — автономная навигация и связь для outdoor-активностей."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.2.1
  flutter_blue_plus: ^2.1.0
  permission_handler: ^12.0.1
  geolocator: ^14.0.2
  android_intent_plus: ^6.0.0
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  # Referenced by analysis_options.yaml
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
